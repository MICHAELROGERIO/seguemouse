let cor;
let posicaoHorizontal; // x
let posicaoVertical; // y

function setup() {
  createCanvas(400, 400);
   background(color(2200,80,1800));
  cor = color(random(0,220), random(0,220), random(0,220));
  posicaoHorizontal = 200;
  posicaoVertical = 200;
}
